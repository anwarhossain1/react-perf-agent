# Option A (default): no key needed - the agent drives the Claude Code CLI,
# which authenticates with your own Claude account via `claude` -> /login.
# Option B: uncomment to drive the Anthropic API directly instead.
# ANTHROPIC_API_KEY=sk-ant-...
